{
  "name": "central-backend",
  "version": "0.1.0",
  "description": "Central backend for the agent_patches fleet console — aggregates state from endpoint agents and serves the central-ui frontend.",
  "type": "module",
  "main": "src/index.js",
  "scripts": {
    "start": "node --import ./src/setup.js src/index.js",
    "dev": "node --import ./src/setup.js --watch src/index.js",
    "test": "node --test"
  },
  "engines": {
    "node": ">=20"
  },
  "dependencies": {
    "cors": "^2.8.5",
    "csv-parse": "^6.2.1",
    "dotenv": "^16.4.5",
    "express": "^4.19.2",
    "nodemailer": "^9.0.1",
    "openai": "^6.44.0",
    "ws": "^8.21.0"
  }
}
