[defaults]
# Accept new host keys automatically (adds to known_hosts on first connect).
# Still fails if a previously-seen key changes, protecting against MITM.
host_key_checking = True

[ssh_connection]
ssh_args = -o StrictHostKeyChecking=accept-new
